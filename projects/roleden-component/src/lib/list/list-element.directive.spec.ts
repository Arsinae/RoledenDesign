import { ListElementDirective } from './list-element.directive';

describe('ListElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ListElementDirective();
    expect(directive).toBeTruthy();
  });
});
